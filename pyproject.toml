[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
# Notebooks are verified by executing them end-to-end (jupyter nbconvert),
# not by the linter, so they are excluded here.
extend-exclude = ["*.ipynb"]
